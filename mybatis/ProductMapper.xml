<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="pdt">
  	<select id="salePdtList" resultType="problem.marcket.ProductDTO">
  		SELECT * FROM tbl_product
  		WHERE cnt > 0
  	</select>
  	
  	<select id="selectPdt" resultType="problem.marcket.ProductDTO">
  		SELECT * FROM tbl_product
  	</select>
  	
  	<select id="searchPdt" resultType="problem.marcket.ProductDTO">
  		SELECT * FROM tbl_product
  		WHERE pname LIKE #{pname}
  	</select>
  	
  	<select id="already" resultType="Integer">
  		SELECT COUNT(*) FROM tbl_product
  		WHERE pname LIKE #{pname}
  	</select>
  	
  	<update id="cntPlus" parameterType="map">
  		UPDATE tbl_product SET
  			cnt = cnt + #{cnt}
  		WHERE pname = #{pname}
  	</update>
  	
  	<update id="salePdt" parameterType="map">
  		UPDATE tbl_product SET
  			cnt = cnt - #{cnt}
  		WHERE pname = #{pname}
  	</update>
  	
  	<update id="updatePdt" parameterType="problem.marcket.ProductDTO">
  		UPDATE tbl_product SET
  			company = #{company},
  			price = #{price},
  			cnt = #{cnt}
  		WHERE pname = #{pname}
  	</update>
  	
  	<insert id="insert" parameterType="problem.marcket.ProductDTO">
  		INSERT INTO tbl_product(pno, pname, company, price, cnt)
  		VALUES(seq_product.NEXTVAL, #{pname}, #{company}, #{price}, #{cnt})  		
  	</insert>
  	
  	<delete id="deletePdt" parameterType="String">
  		DELETE FROM tbl_product
  		WHERE pname = #{pname}
  	</delete>
  	
  </mapper>